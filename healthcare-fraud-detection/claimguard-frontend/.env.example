# Point this at your Flask/FastAPI backend once it's live
VITE_API_BASE_URL=http://localhost:8000/api

# true  = every service call returns mock data (no backend needed, demo accounts on Login)
# false = every service call hits VITE_API_BASE_URL using the real endpoints in the PRD
VITE_USE_MOCK=true
